weathertrip-api/
├── README.md               # pitch, Mermaid diagram, quickstart, endpoint table
├── docker-compose.yml      # app1 + app2 + mariadb on a bridge network
├── .env.example            # DB_USER=weathertrip, DB_PASSWORD=changeme, etc.
├── .gitignore              # .env, __pycache__/, *.pyc
├── LICENSE                 # MIT — it's your code
│
├── app1/
│   ├── Dockerfile          # reconstructed from docker history
│   ├── requirements.txt    # recovered from the image
│   ├── wsgi.py             # recovered
│   ├── weather_client.py   # recovered
│   └── database.py         # recovered
│
├── app2/
│   ├── Dockerfile
│   ├── requirements.txt
│   └── wsgi.py             # recovered
│
├── db/
│   ├── Dockerfile          # FROM mariadb:11 + COPY schema
│   └── schema.sql          # recovered from /docker-entrypoint-initdb.d/
│
├── docs/
│   ├── api.md              # every endpoint, request/response, real curl transcripts
│   └── design-decisions.md # your writeup, rewritten for a stranger
│
└── infra/
    ├── README.md           # "reconstructed from design notes; originals lived on ephemeral course VMs"
    ├── nginx/weathertrip.conf      # 80→443 redirect, TLS, upstream to .51/.53
    ├── nftables/nftables.conf      # iifname ens3 → 22/80/443, 53 tcp+udp, drop rest
    ├── netplan/router.yaml         # ens3 WAN, ens4 .49, route for .50 via docker2
    ├── netplan/docker-node.yaml    # dhcp4: false, static addressing
    ├── wireguard/wg0-server.conf.example   # AllowedIPs .48/29, keys as REDACTED
    ├── wireguard/wg0-client.conf.example   # no client IP, forwarding note
    ├── dns/                        # your address entries for final/docker1-3/aws
    └── sysctl.d/99-forwarding.conf # ip_forward=1, proxy_arp on ens4
